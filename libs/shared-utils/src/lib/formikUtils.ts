import { FormikConfig, FormikValues } from 'formik'

/** */
export function formikToComponentProps<T extends FormikValues> (formikConfig: FormikConfig<T>) {
  const { } = formikConfig;
  return {

  }
}