query findConProfile($conProfileId: ID!) {
  conProfile(id: $conProfileId) {
    userId
    id
    firstName
    lastName
    languages
    categories
    rediLocation
    profileAvatarImageS3Key
    email
    telephoneNumber
    githubProfileUrl
    linkedInProfileUrl
    slackUsername
  }
}
