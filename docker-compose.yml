version: '3.1'
services:
  # mongodb:
  #   image: mongo
  #   container_name: mongodb
  #   restart: always
  #   ports:
  #     - '27017:27017'
  #   volumes:
  #     - ./mongodb-data:/data/db

  nestjs:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - '3333:3333'
    env_file: .env
